scandelay = 1;
retry_count = 5;
samplerate = 10;

# In binary: 11
channel_mask = 3;

HSH2: {
	ledindex = 0;

	# The @include statement has to stand in its
	# own row.
	channels: ({
			@include "sensor_normal.cfg"
		}, {
			@include "sensor_normal.cfg"
		}
	)
}
HSH7: {
	ledindex = 1;
	channels: ({
			@include "sensor_normal.cfg"
		}, {
			@include "sensor_normal.cfg"
		}
	)
}

HSH10: {
	ledindex = 2;
	channels: ({
			@include "sensor_normal.cfg"
		}, {
			@include "sensor_normal.cfg"
		}
	)
}
HSH16: {
	ledindex = 3;
	channels: ({
			@include "sensor_normal.cfg"
		}, {
			@include "sensor_normal.cfg"
		}
	)
}